interface:
  display_name: "Email Organizer"
  short_description: "Extract attachments, create PDFs, and deduplicate emails"
  default_prompt: "Use $email-organizer to organize these EML files, save all unique attachments, and retain the newest complete email chains."
