interface:
  display_name: "Email Organizer"
  short_description: "Extract attachments, save email text, and remove duplicates"
  default_prompt: "Use $email-organizer to extract attachments and save email chains as TXT in the same folder, including attached emails, then clean up older duplicates. Generate PDF only if requested."
